interface:
  display_name: "Embedded C Host Validation"
  short_description: "仅按用户指定的 C 功能块执行 GCC/Mock 主机仿真验证"
  default_prompt: "Use $embedded-c-host-validation to host-simulate the specific embedded C function or module I name after a code change."
